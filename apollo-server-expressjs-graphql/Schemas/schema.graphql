type Query {
  hello: String,
  age: Int!,
  posts: [Post]!,
  users: [User]
}

type Post {
  id: ID!
  title: String!
  body: String!
}

type User {
  id: ID!
  name: String 
  email: String 
  phone: String
}